# Default values for society-server.

# Tags allow to enable/disable certain components of society-server as defined in requirements.yaml
# https://docs.helm.sh/developing_charts/#tags-and-condition-fields-in-requirements-yaml
# tags:
#   services: true

tags:
  team-settings: false
  account-pages: true
